export const GameMode = Object.freeze({
  HumanVsComputer: 'HumanAI',
  HumanVsHuman: 'success',
  ComputerVsComputer: 'error',
});

export const GameRole = Object.freeze({
    Hider: "hider",
    Seeker: "Seeker",
});

export const PlayerType = Object.freeze({
    Human: "Human",
    Computer: "Computer",
})